# pnpm configuration
enable-pre-post-scripts=true

# Disable build script restrictions (allow all packages to run build scripts)
# This is required for packages like Prisma, esbuild, etc.
ignore-scripts=false
